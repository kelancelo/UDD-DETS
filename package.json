{
  "name": "dets",
  "version": "1.0.0",
  "description": "1) Open terminal on this project's directory.\r 2) Type `python manage.py runserver`\r 3) Visit \r     * http://127.0.0.1:8000/ for the main site\r     * http://127.0.0.1:8000/admin for the admin site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.3",
    "tailwindcss": "^3.3.2"
  }
}
